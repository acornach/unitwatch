<h1>Unitlist</h1>
<form>
  <input type="text" ng-model='newSymbol' placeholder="Enter a symbol">
  <input type="submit" value="Add">
</form>
<div ng-controller="UnitsCtrl">
  <ul>
    <li ng-repeat="unit in units">
      {{unit.title}}
	  <a href = '' ng-click="deleteUnit(unit.id,$index)">Delete</a>
    </li>
  </ul>
</div>